# Issue #149: Pre-migration baseline: orphan maintained CLI regression tests

- Original URL: https://github.com/tigger-developer/superscale/issues/149
- Snapshot state: open
- State reason: none recorded
- Labels: none
- Milestone: none
- Author: tigger-developer
- Created: 2026-09-03T02:10:16Z
- Closed: not closed at snapshot

## Code-commit references

None recorded.

## Original issue body

## Purpose

This pre-migration baseline records three maintained regression tests that have no resolvable legacy ticket or acceptance-criterion provenance.

It introduces no new implementation scope. It gives the existing observable behaviour durable provenance before the legacy ticket system is retired.

## Observable acceptance criteria for migration

The migration ledger will assign identifiers from this issue number.

1. Invoking `superscale --help` exits successfully and prints usage information.
2. Invoking `superscale --list-models` exits successfully and lists the default `realesrgan-x4plus` model.
3. Invoking `superscale` without an input exits unsuccessfully.

## Maintained regression evidence

- **RT-002: help invocation succeeds.** `Tests/SuperscaleTests/CLITests.swift`, `test_cli_help_flag_returns_zero`.
- **RT-003: model listing succeeds.** `Tests/SuperscaleTests/CLITests.swift`, `test_cli_list_models_returns_zero`.
- **RT-004: missing input is rejected.** `Tests/SuperscaleTests/CLITests.swift`, `test_cli_no_input_returns_error`.

## Current evidence

The operator supplied current passing-suite authority on 2026-09-03. The migration reuses that authority and does not execute the suite.


## Comments

No comments were recorded.

